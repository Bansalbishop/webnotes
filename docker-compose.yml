version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: notes-web:latest
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
